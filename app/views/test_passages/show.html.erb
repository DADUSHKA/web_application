<div id="container">
  <div id="header">TestGuru</div>
<div id="sidebar">
    <p> <%= t('.number') %></p>
    <%=  @test_passage.indicator_number_question(@test_passage)  %></br>
    <p> <%= t('.count') %></p>
    <%=  @test_passage.test.questions.count %></br>
  </div>
  <div id="content">
    <h2><%= t('.header', title: @test_passage.test.title) %></h2>
    <p>
      <h2><%= t('.question') %></h2>
      <strong><%= @test_passage.question.description %></strong>
    </p>
    <%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form|  %>
    <%= form.collection_check_boxes :answer_ids, @test_passage.question.answers, :id, :body, include_hidden: false do |b| %>
    <p>
      <%= b.check_box id: "answer_ids_#{b.object.id}"  %>
      <%= b.label  %>
    </p>
    <% end  %>
    <p>
      <%= form.submit t('.next')  %>
    </p>
    <% end %>
 </div>
</div>

