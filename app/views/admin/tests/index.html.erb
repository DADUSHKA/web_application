  <div class="row mb-2">
    <div class="col-md-12 ">
      <div class="flex-md-row mb-4 box-shadow z">
        <div class="card-body d-flex flex-column align-items-start">
          <nav class="navbar navbar-light">
            <ul class="nav justify-content-end">
              <li class="nav-item">
                <a class="nav-link" href="#"><%= link_to 'Gist',
                admin_gists_index_path %></a>&nbsp;&nbsp;
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#"><%= link_to t('.new_category'),
                new_admin_category_path %></a>&nbsp;&nbsp;
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#"><%= link_to t('.new_test'),
                new_admin_test_path %></a>
              </li>
            </ul>
          </nav>
          <h2><%= t('.header') %></h2>
          <div class="table-responsive">
            <table class="table table-striped table-sm">
              <thead>
                <tr>
                 <th><%= t('.title') %></th>
                 <th><%= t('.level') %></th>
                 <th><%= t('.author') %></th>
                 <th><%= t('.category') %></th>
                 <th><%= t('.total_question') %></th>
                 <th colspan="3"><%= t('.actions') %></th>
               </tr>
             </thead>
             <tbody>
              <%= render @tests %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
